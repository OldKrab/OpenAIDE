# Tauri is the Desktop framework

OpenAIDE Desktop uses Tauri 2 as its permanent native App Shell, sharing the existing Frontend and bundled App Server across Windows and macOS. Framework hardening now happens through ordinary Desktop feature and release work rather than a provisional Tauri-versus-Electron validation program; investigate another framework only when a concrete product requirement cannot be met safely with Tauri.
