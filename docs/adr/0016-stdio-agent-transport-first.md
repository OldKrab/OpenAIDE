# Stdio Agent Transport First

OpenAIDE will implement ACP stdio process Agents first. This supports the Codex built-in through `codex-acp` and Custom process Agents without delaying the first iteration on draft HTTP/WebSocket transports; HTTP/WebSocket remain tracked ACP draft support and must fit the same Agent, auth, Configuration Option, Task, and Host bridge contracts when added.
