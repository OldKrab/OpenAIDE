# Skills Managed as Metadata First

In the first iteration, OpenAIDE will scan, display, validate, enable, disable, and diagnose Skills in Settings, but it will not automatically inject Skill content into prompts or execute Skills through the Agent runtime. This keeps ACP Task behavior explicit and prevents hidden prompt mutation; future explicit skill attachment or invocation can be designed as a separate feature.
